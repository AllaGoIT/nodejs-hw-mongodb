# swagger/components/responses/404.yaml

description: error Not found contact
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: Bad Rquest
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Contact not found'
