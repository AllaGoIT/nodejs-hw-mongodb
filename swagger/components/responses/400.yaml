description: error '.exe not valid extension or bad request'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: '.exe not valid extension or bad request'
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: '.exe not valid extension or bad request'
